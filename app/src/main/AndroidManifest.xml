<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.funchv3">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FunchV3"
        tools:targetApi="31">

        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->


        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".maps.MapsActivity2"
            android:exported="false"
            android:label="@string/title_activity_maps"/>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".User.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Visitor.signup"
            android:exported="false" />
        <activity
            android:name=".User.login"
            android:exported="false" />
        <activity
            android:name=".User.forgetPassword"
            android:exported="false" />
        <activity
            android:name=".User.homepage"
            android:exported="false" />
        <activity
            android:name=".Visitor.homeVisitor"
            android:exported="false" />
        <activity
            android:name=".User.listPage"
            android:exported="false" />
        <activity
            android:name=".User.filterPage"
            android:exported="false" />
        <activity
            android:name=".User.randomize"
            android:exported="false" />
        <activity
            android:name=".User.restaurantComments"
            android:exported="false" />
        <activity
            android:name=".User.updateProfile"
            android:exported="false" />
        <activity
            android:name=".User.myProfile"
            android:exported="false" />
        <activity
            android:name=".User.changePassword"
            android:exported="false" />
    </application>

</manifest>